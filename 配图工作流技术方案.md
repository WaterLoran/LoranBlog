# 博客配图工作流 · 技术方案与实施计划

> 状态：**待评审，未实施**
> 目标：把「生成 → 压缩 → 插入引用」固化为一条命令，并落实配图规范（WebP / 尺寸上限 / 数量控制 / 懒加载），保证 2U4G50G 服务器 + 低带宽下的加载体验。

---

## 1. 背景与约束

| 项 | 现状 / 约束 |
|---|---|
| 文档规模 | 898 篇 Markdown，无 front matter，纯 `# 标题` 开头 |
| 图片托管 | `docs/.vuepress/public/images/`，文中以 `/images/...` 绝对路径引用 |
| 部署方式 | 本地 `yarn docs:build`，dist 整个推送服务器 |
| 服务器 | 2 核 4G 内存 50G 磁盘，带宽约 1~5 Mbps（**唯一真实瓶颈**） |
| 生成能力 | image_generation 插件脚本（`image_generation_tool.py`），支持比例/分辨率参数 |
| 实测压缩收益 | PNG 2.4MB → WebP(q80, 1200px) ≈ 120KB，**缩小约 95%** |

**性能预算（验收红线）**：
- 单页图片总量 ≤ 500 KB（普通文章 ≤ 150 KB）
- 3 Mbps 带宽下，单页图片加载 ≤ 2 秒
- 全站配图完成后，dist 图片目录增量 ≤ 500 MB

## 2. 配图规范（落地口径）

| 规范 | 细则 |
|---|---|
| 格式 | 一律 WebP，质量 80 |
| 尺寸 | 头图宽 ≤ 1200px（16:9）；正文插图宽 ≤ 800px（3:2） |
| 数量 | 普通文章 1 张头图；重点长文最多 3~4 张（1 头图 + 2~3 章节图） |
| 加载 | 头图正常加载（首屏）；其余图片一律 `loading="lazy"` |
| 原图 | PNG 原图保留在 `assets-src/images/<文章 slug>/`（**不放进 public，不上服务器**） |
| 命名 | 图片目录与文章同名 slug：`/images/<文章名>/banner.webp`、`section-01.webp` … |
| 构建 | public 原样拷贝，图片增多不影响编译耗时；部署流程不变（推 dist） |

## 3. 脚本设计：`tools/illustrate.py`

一条命令完成全流程：

```bash
# 普通文章：只生成 1 张头图
python3 tools/illustrate.py "docs/测试管理/工作方式/某文章.md" --mode banner

# 重点长文：头图 + 按章节生成插图（自动读取 ### 小节，最多 3 张）
python3 tools/illustrate.py "docs/测试管理/工作方式/某文章.md" --mode full --sections 3

# 不给提示词时，脚本读取文章标题和小节标题，自动生成描述（风格词内置，保证全站画风统一）
python3 tools/illustrate.py "docs/xxx.md" --mode full --prompt "自定义风格或主题提示词"
```

### 3.1 处理流程

```
读取 .md ──► 解析 H1 标题 + ### 小节列表
    │
    ▼
组装生成描述（内置统一风格后缀，如 "cinematic editorial illustration,
deep navy blue and fiery orange palette, dark tech atmosphere"）
    │
    ▼
调用 image_generation_tool.py generate
  · banner: 16:9, 2K
  · section: 3:2, 1K
    │
    ▼
PNG 原图归档到 assets-src/images/<slug>/
    │
    ▼
Pillow 压缩：banner → 1200px WebP q80；section → 800px WebP q80
输出到 docs/.vuepress/public/images/<slug>/
    │
    ▼
改写 Markdown（幂等，可重复执行）：
  · H1 后插入：![<标题>](/images/<slug>/banner.webp)
  · 各 ### 小节末尾插入：
    <img src="/images/<slug>/section-0N.webp" alt="<小节标题>" loading="lazy" />
    │
    ▼
输出报告：生成几张、各多少 KB、插入位置行号
```

### 3.2 关键设计点

1. **幂等性**：插入前检测 `/images/<slug>/` 引用是否已存在，存在则跳过或 `--force` 重新生成，防止重复配图。
2. **懒加载不用插件**：直接用 `<img loading="lazy">` 原生标签。理由：
   - 零依赖，不引入 `vuepress-plugin-img-lazy`（该插件多年未维护，与 VuePress 1.9 + reco 主题兼容性需额外验证）；
   - 原生懒加载所有现代浏览器均支持；
   - 头图保持 `![]()` 语法以保证首屏立即加载。
3. **slug 规则**：取文章文件名（去 `.md`），与现有 `images/AgenticAI基础设施实践经验/` 中文目录惯例一致，URL 无需额外编码处理（VuePress 与 serve 均已验证支持中文路径）。
4. **失败处理**：某张图生成失败时跳过该张并记录，不回滚已插入的引用；脚本退出码非 0 并打印待重试清单。
5. **同步 dist（可选开关）**：`--sync-dist` 时把 WebP 拷入 `dist/images/` 并改写对应 HTML 引用，免去整站重编译即可本地预览（本次 musk-efficiency 就是这么做的）。

### 3.3 依赖

- Pillow（托管 Python 已自带，无需安装）
- image_generation 插件脚本（已就绪）
- 无新增 npm 依赖

## 4. 实施计划（分期）

| 阶段 | 内容 | 验收标准 | 预估工作量 |
|---|---|---|---|
| **P1 · 脚本骨架** | 实现 `tools/illustrate.py` 的 banner 模式（生成→压缩→插入→报告），拿 1 篇新文章试跑 | 一条命令跑通；产物 ≤ 150KB；重复执行不重复插入 | 0.5 天 |
| **P2 · full 模式** | 支持按 `###` 小节自动配图（≤3 张，800px，lazy）+ `--sync-dist` | 长文 4 张图总量 ≤ 500KB；懒加载生效（DevTools Network 验证） | 0.5 天 |
| **P3 · 存量重点文章批量配图** | 按分级策略 A：先给 nav/sidebar 中曝光度高的约 100 篇配头图 | dist 图片增量 ≤ 25MB；抽查页面加载正常 | 分批进行，脚本自动跑 |
| **P4 · 存量图片治理（可选）** | 把 `AgenticAI基础设施实践经验` 等存量 PNG/JPG 批量转 WebP，更新引用 | 现有 64MB 图片目录压缩至 ≤ 15MB | 0.5 天 |
| **P5 · 远期（可选）** | 图片总量超 1GB 后迁移对象存储 + CDN，脚本加 `--upload` 开关 | 服务器只放 HTML/JS | 另行评估 |

**建议先做 P1 + P2，跑几篇文章验证效果和手感，再决定是否启动 P3 批量。**

## 5. 风险与备注

| 风险 | 应对 |
|---|---|
| 批量生成画风漂移 | 风格后缀内置在脚本里，所有图共享同一套风格词 |
| 生成成本（898 篇全配） | 按规范只配头图 ≈ 900 次生成调用，建议分批；重点文章才用 full 模式 |
| 中文路径在某些服务器环境出问题 | 当前 serve/Nginx 已验证 OK；若未来换环境，脚本可加 `--slug pinyin` 开关 |
| `<img>` 原生标签与 reco 主题样式 | P1 阶段试跑时检查渲染效果，必要时在 config.js 的 head 里补一条全局 CSS |
| dist 手动同步与下次全量编译的一致性 | 以 `yarn docs:build` 为最终口径，`--sync-dist` 仅用于本地快速预览 |
