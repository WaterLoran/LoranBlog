# Skill的输入输出设计

总结一下 **Cursor / Claude Code 中 Skill 的输入输出设计优秀理论与经验**，并附上具体案例。

---

## 一、核心设计理念（一句话总结）

> **Skill 的输入输出边界 = 清晰约定 + 最小依赖 + 可预期行为 + 无害默认**

每个 Skill 都要回答三个问题：
1. **从哪里获取信息？**（输入来源）
2. **能做什么，不能做什么？**（能力边界）
3. **产出什么，以什么形式？**（输出规格）

---

## 二、输入设计优秀实践（5 条）

### 1. **显式声明输入来源，避免“魔法猜测”**
- **理论**：Skill 只能依赖用户明确提供或项目约定的上下文，不能臆测。
- **做法**：在 Skill 提示词中列出支持的输入类型（当前文件、选中代码、参数、配置文件等），并说明优先级。

**示例**（代码审查 Skill）：
```yaml
输入约定：
1. 优先使用用户通过 `/review` 命令后粘贴的代码块。
2. 若无代码块，则使用当前编辑器中选中的代码。
3. 若未选中，则使用当前打开文件的全部内容。
4. 若文件为空，回复“请提供代码”。
5. 忽略项目中其他未提及的文件。
```

### 2. **支持参数化输入，增强复用性**
- **理论**：允许用户通过 `--key value` 或自然语言传递参数，使 Skill 适配不同场景。
- **做法**：定义清晰的参数语法，并在提示词中解释每个参数的作用。

**示例**（日志分析 Skill）：
```
用法：/logscan [file] [--level ERROR|WARN] [--lines N]
- 若未指定 file，默认读取 ./logs/app.log
- --level 过滤日志级别，默认全部
- --lines 只显示最后 N 行，默认 100
```

### 3. **输入必须可校验，失败时给出明确错误提示**
- **理论**：Skill 不能因为输入错误而“胡乱回答”，必须优雅地拒绝并告知原因。
- **做法**：在提示词中加入输入校验规则和错误处理流程。

**示例**（SQL 执行 Skill）：
```markdown
## 输入校验
- 如果提供的 SQL 语句不包含 SELECT（仅允许只读查询），Skill 必须回复：
  “本 Skill 仅支持 SELECT 查询，不允许修改操作。请检查您的 SQL。”
- 如果数据库连接失败，回复：“无法连接到数据库，请检查 .dbconfig 文件。”
```

### 4. **依赖项目中的约定文件时，明确其路径和格式**
- **理论**：如果 Skill 需要读取项目内的特定文件（如 `.eslintrc`、`pom.xml`），必须显式说明，避免模糊依赖。
- **做法**：在 Skill 描述中写出文件名、预期位置、解析方式。

**示例**（自动修复代码风格 Skill）：
```
本 Skill 会尝试读取项目根目录下的以下文件：
- .editorconfig（用于缩进和换行符）
- .prettierrc（用于格式化规则）
如果找不到，将使用默认规则（2 空格缩进，无分号）。
```

### 5. **允许用户覆盖默认输入，提供“静默模式”**
- **理论**：高级用户希望能跳过交互，直接得到结果。
- **做法**：支持 `--quiet` 或 `--yes` 参数，跳过确认步骤。

---

## 三、输出设计优秀实践（6 条）

### 1. **区分“主要输出”和“辅助输出”**
- **理论**：主要输出是 Skill 的核心产物（分析结果、生成代码、文件修改建议）；辅助输出是日志、中间步骤、调试信息。
- **做法**：主要输出放在回答最前面，辅助输出可以折叠或用代码块注明。

**示例**：
```
【主要输出】检测到 3 个性能问题：
1. 循环内调用数据库查询...
2. 未使用索引...

【辅助信息】分析耗时 0.3 秒，检查了 12 个文件。
```

### 2. **输出类型限定为安全、可逆的操作**
- **理论**：Skill 默认不应有破坏性输出（如直接删除文件、执行不可逆命令）。如果需要，必须征得用户明确同意。
- **做法**：在提示词中声明“本 Skill 不会修改任何文件，除非用户回复‘确认应用’”。

**示例**（重构 Skill）：
```markdown
## 输出行为
- 本 Skill 只生成重构建议的代码块（Markdown 格式）。
- 不会自动替换原文件。
- 如果用户希望应用修改，需要手动复制代码或输入“应用第 X 个建议”，Skill 会再次确认后给出替换指令。
```

### 3. **输出文件时遵循“询问路径 + 可预期命名”原则**
- **理论**：避免文件散落在各处或覆盖已有文件。
- **做法**：先问用户保存路径；若用户不指定，使用当前工作目录 + `skill_name_YYYYMMDD_HHMMSS.ext`。

**示例**（生成测试报告 Skill）：
```
用户：/report
AI：请指定报告保存路径（例如 ./reports/），或回复“默认”将保存到当前目录下 `report_20250315_143022.md`。
用户：默认
AI：报告已生成：/home/project/report_20250315_143022.md
```

### 4. **结构化输出便于后续 Skill 链式调用**
- **理论**：如果多个 Skill 可以串联使用，输出应采用 JSON/YAML 等结构化格式，并约定字段。
- **做法**：提供 `--json` 输出选项，专供机器解析。

**示例**（代码复杂度分析 Skill）：
```bash
/complexity --json src/main.py
# 输出
{"cyclomatic_complexity": 15, "lines": 120, "maintainability_index": 68}
```
另一个 Skill 可以直接读取该输出。

### 5. **输出中附带“下一步建议”，引导用户**
- **理论**：好的 Skill 不仅完成当前任务，还告诉用户接下来能做什么。
- **做法**：在回答末尾给出 1~2 个相关 Skill 或操作建议。

**示例**：
```
...（分析结果）
💡 下一步建议：
- 使用 `/refactor` 自动重构上述高复杂度函数
- 使用 `/doc generate` 为新增的接口补充注释
```

### 6. **错误输出要提供可操作的修复建议**
- **理论**：出错时不是只报“失败”，而要给出原因和解决方法。
- **做法**：错误信息包含检查点、文档链接或修复命令。

**示例**：
```
❌ 无法解析 requirements.txt。
原因：文件编码不是 UTF-8。
修复：运行 `iconv -f gbk -t utf-8 requirements.txt > requirements_utf8.txt` 后重试。
```

---

## 四、完整案例：设计一个“单元测试生成” Skill（汇总上述实践）

### Skill 定义（伪代码 / 提示词）

```yaml
name: gen-unit-test
trigger: /test [file] [--framework=jest|pytest|go test] [--json]
description: 为指定文件生成单元测试代码。

## 输入边界
- 输入来源优先级：
  1. 命令中的 file 参数（如 /test src/util.js）
  2. 当前编辑器中打开的文件
  3. 用户选中的代码片段（仅测试该片段）
- 可选框架参数：--framework，默认根据文件扩展名猜测（.js→jest, .py→pytest）
- 自动读取项目根目录下的测试配置文件（如 jest.config.js, pytest.ini）以了解别名、覆盖率要求等。

## 输出边界
- 默认输出：在对话中显示测试代码块（Markdown 格式），不自动创建文件。
- 若用户明确要求保存文件（如 /test --save），则：
  - 询问目标路径（建议同目录下 `*.test.js` 或 `test_*.py`）
  - 得到确认后写入，并回复“已保存到 <路径>”
- 若包含 --json 参数，输出 JSON 格式：{"test_code": "...", "framework": "jest", "coverage_estimate": 85}
- 绝不修改原代码文件。

## 错误处理
- 如果无法推断测试框架，列出可能的框架并要求用户指定。
- 如果文件无法解析（语法错误），提示具体行号和修复建议。
- 如果生成测试时遇到无法 mock 的外部依赖，标注 `// TODO: mock this` 并给出手动修改提示。

## 示例对话
用户：/test src/calculator.js
AI：检测到 Jest 框架，生成了 5 个测试用例。代码块如下：
```javascript
test('adds 1+2 to equal 3', () => { ... })
...
```
是否需要保存到 `src/calculator.test.js`？
用户：是
AI：已写入文件。运行 `npm test` 执行测试。
```

---

## 五、总结对照表

| 方面 | 优秀实践 | 错误示例 |
|------|----------|----------|
| 输入来源 | 明确优先级，显式列出依赖的文件/上下文 | “读取相关文件”（太模糊） |
| 参数化 | 支持 `--key value`，有默认值 | 硬编码参数在提示词中 |
| 输入校验 | 校验失败时给出具体修复步骤 | 只回复“输入无效” |
| 输出形式 | 默认只输出文本，文件输出需确认 | 直接覆盖用户文件 |
| 文件命名 | 询问路径 + 时间戳命名 | 固定 `/tmp/result` |
| 结构化输出 | 提供 `--json` 用于链式调用 | 只能输出自然语言 |
| 错误处理 | 说明原因 + 修复建议 | 抛出晦涩的异常 |
| 下一步提示 | 主动推荐后续 Skill | 回答完就结束 |

这些理论和经验来自实际使用 Cursor/Claude Code 的教训总结。遵循它们，你的 Skill 将变得**可靠、可组合、用户友好**。